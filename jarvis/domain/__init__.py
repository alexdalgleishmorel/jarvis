"""Pure domain core — entities, value objects, and domain logic.

Load-bearing rule (README §3.1, §13): this package imports **nothing** from
``jarvis.adapters`` or ``jarvis.app`` and depends on no third-party libraries. It
is plain Python with no I/O.
"""
